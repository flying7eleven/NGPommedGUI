language: cpp

compiler:
  - clang

before_install:
  - sudo add-apt-repository ppa:boost-latest/ppa -y
  - sudo apt-get update

install:
  - sudo apt-get install libboost1.55-dev

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_INSTALL_PREFIX=../.install -DCMAKE_BUILD_TYPE=Debug $OPTIONS ..
  - cmake --build . --target install

branches:
  only:
    - master

os:
  - linux
